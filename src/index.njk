---
layout: base.njk
title: Home
description: Modern digital marketing agency specializing in brand strategy and creative solutions
---

<section class="hero">
  <img class="hero__background" src="https://images.unsplash.com/photo-1460925895917-afdab827c52f" alt="Modern Marketing Agency">
  <div class="hero__content">
    <h1 class="animate-on-scroll">Modern Marketing Agency</h1>
    <p class="animate-on-scroll">We create digital experiences that transform brands</p>
    <a href="#contact" class="button animate-on-scroll">Get in Touch</a>
  </div>
</section>

<section class="container">
  <div class="grid">
    <div class="animate-on-scroll">
      <h2>Strategy</h2>
      <p>Data-driven marketing strategies that deliver results</p>
    </div>
    <div class="animate-on-scroll">
      <h2>Design</h2>
      <p>Beautiful, functional designs that engage users</p>
    </div>
    <div class="animate-on-scroll">
      <h2>Development</h2>
      <p>Cutting-edge technology solutions for modern brands</p>
    </div>
  </div>
</section>

<section class="featured-section">
  <div class="container">
    <div class="featured-section__header">
      <h2>Featured Projects</h2>
      <p>Explore some of our recent work and success stories</p>
    </div>
    <div class="grid">
      {%- for project in collections.projects | reverse | limit(2) -%}
        <article class="project-card animate-on-scroll">
          {% if project.data.featuredImage %}
            <img src="{{ project.data.featuredImage }}" alt="{{ project.data.title }}">
          {% endif %}
          <div class="project-card__content">
            <h2>{{ project.data.title }}</h2>
            <p class="project-card__client">{{ project.data.client }}</p>
            <div class="project-card__tags">
              {% for tag in project.data.tags %}
                <span class="tag">{{ tag }}</span>
              {% endfor %}
            </div>
            <a href="{{ project.url }}" class="button">View Project</a>
          </div>
        </article>
      {%- endfor -%}
    </div>
  </div>
</section>

<section class="featured-section">
  <div class="container">
    <div class="featured-section__header">
      <h2>Latest Insights</h2>
      <p>Stay updated with our latest thoughts on marketing and design</p>
    </div>
    <div class="grid">
      {%- for post in collections.posts | reverse | limit(2) -%}
        <article class="post-card animate-on-scroll">
          {% if post.data.featuredImage %}
            <img src="{{ post.data.featuredImage }}" alt="{{ post.data.title }}">
          {% endif %}
          <div class="post-card__content">
            <h2>{{ post.data.title }}</h2>
            <time datetime="{{ post.date | dateToISO }}">{{ post.date | date("MMMM D, YYYY") }}</time>
            <p>{{ post.data.excerpt }}</p>
            <div class="post-card__tags">
              {% for tag in post.data.tags %}
                <span class="tag">{{ tag }}</span>
              {% endfor %}
            </div>
            <a href="{{ post.url }}" class="button">Read More</a>
          </div>
        </article>
      {%- endfor -%}
    </div>
  </div>
</section>

<section class="lead-magnet container">
  <div class="lead-magnet__grid">
    <div class="lead-magnet__content">
      <h2>Get Our Marketing Insights</h2>
      <p>Subscribe to our newsletter for the latest trends, strategies, and insights in digital marketing.</p>
      <form class="lead-magnet__form" name="newsletter" method="POST" data-netlify="true">
        <input type="email" name="email" placeholder="Enter your email" required>
        <button type="submit" class="button">Subscribe</button>
      </form>
    </div>
    <div class="lead-magnet__image">
      <img src="https://images.unsplash.com/photo-1434626881859-194d67b2b86f" alt="Marketing Newsletter">
    </div>
  </div>
</section>