---
layout: base.njk
title: Blog
description: Insights and thoughts on digital marketing, design, and technology
eleventyExcludeFromCollections: true
---

<header class="page-header">
  <div class="container container--narrow">
    <h1>Blog</h1>
    <p>Latest insights and industry updates</p>
  </div>
</header>

<section class="container">
  <div class="grid">
    {%- for post in collections.posts | reverse -%}
      <article class="post-card animate-on-scroll">
        {% if post.data.featuredImage %}
          <img src="{{ post.data.featuredImage }}" alt="{{ post.data.title }}">
        {% endif %}
        <div class="post-card__content">
          <h2>{{ post.data.title }}</h2>
          <time datetime="{{ post.date | dateToISO }}">{{ post.date | date("MMMM D, YYYY") }}</time>
          <p>{{ post.data.excerpt }}</p>
          <div class="post-card__tags">
            {% for tag in post.data.tags %}
              <span class="tag">{{ tag }}</span>
            {% endfor %}
          </div>
          <a href="{{ post.url }}" class="button">Read More</a>
        </div>
      </article>
    {%- endfor -%}
  </div>
</section>