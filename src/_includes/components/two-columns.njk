{% if componentData.type == "twoColumns" %}
  <div class="two-columns container">
    <div class="two-columns__grid">
      {% for column in componentData.columns %}
        <div class="two-columns__item">
          {{ column.content | markdown | safe }}
        </div>
      {% endfor %}
    </div>
  </div>
{% endif %}