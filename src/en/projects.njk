---
layout: base.njk
title: Projects
description: Our portfolio of client work and case studies
permalink: "/projects/"
locale: en
eleventyExcludeFromCollections: true
---

<header class="page-header">
  <div class="container container--narrow">
    <h1>Our Projects</h1>
    <p>Explore our latest work and success stories</p>
  </div>
</header>

<section class="container">
  <div class="grid">
    {%- for project in collections.projects_en | reverse -%}
      <article class="project-card animate-on-scroll">
        {% if project.data.featuredImage %}
          <img src="{{ project.data.featuredImage }}" alt="{{ project.data.title }}">
        {% endif %}
        <div class="project-card__content">
          <h2>{{ project.data.title }}</h2>
          <p class="project-card__client">{{ project.data.client }}</p>
          <div class="project-card__tags">
            {% for tag in project.data.tags %}
              <span class="tag">{{ tag }}</span>
            {% endfor %}
          </div>
          <a href="{{ project.url }}" class="button">View Project</a>
        </div>
      </article>
    {%- endfor -%}
  </div>
</section>